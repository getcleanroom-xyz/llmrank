name: Ping Render

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API to prevent sleep
        run: curl -sf https://llmrank-0u8o.onrender.com/health > /dev/null 2>&1 || true
